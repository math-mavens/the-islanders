<%= render 'shared/banner' %>

<div class="container-fluid">
  <%= render 'shared/features' %>
  <%= render 'shared/homecards' %>

</div>

<div class="container-fluid">
  <% if user_signed_in? %>
    <%#= current_user.email %>
  <% end %>
</div>
