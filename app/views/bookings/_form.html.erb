<%= simple_form_for [@island, @booking] do |f| %>
  <%= f.input :start_date, placeholder: "Starting from..." %>
  <%= f.input :end_date, placeholder: "...till" %>
  <%= f.submit "Confirm", class: "btn btn-primary" %>
<% end %>
