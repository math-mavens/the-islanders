<div class="container">
  <h1 class="mb-5 mt-5">Manage Your Island</h1>
      <%= render "form" %>
      <%= link_to "Delete this Island", island_path(@island), class: "btn btn-link mb-5 row row-cols-auto", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this island?" } %>
</div>
